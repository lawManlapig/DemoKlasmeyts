using klasmeyts.srv as service from '../srv/services';

annotate service.KlasmeytsService.Klasmeyts with @( //enclose mo ng parenthesis if madami kang annotaion na ilalagay
    odata.draft.enabled
);
